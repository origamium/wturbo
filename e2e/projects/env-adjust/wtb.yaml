# Env Adjust Test wtb Configuration
# Tests env.file + env.adjust functionality

base_branch: "main"
docker_compose_file: "./docker-compose.yml"

copy_files: []

env:
  file:
    - ./.env
  adjust:
    APP_PORT: 1000
    DB_PORT: 1000
    REDIS_PORT: 1000
